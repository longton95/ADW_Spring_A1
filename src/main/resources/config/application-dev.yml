server:
    port: 8080

spring:
    profiles:
        active: dev
    data:
        mongodb:
            uri: mongodb://localhost/springAssignment
            database: springAssignment
            host: localhost
            port: 27017

logging:
    level:
        root: DEBUG
        org.springframework: DEBUG
        org.springframework.web: DEBUG
        org.springframework.security: DEBUG

spring:
  thymeleaf:
    cache: false